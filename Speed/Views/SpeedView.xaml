<UserControl x:Class="Speed.Views.SpeedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:core="clr-namespace:YMM4GameHub.Core.Controls;assembly=YMM4GameHub.Core"
             xmlns:local="clr-namespace:Speed.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <local:ResetVisibleConverter x:Key="ResetVisibleConverter"/>
        <Style x:Key="CardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="RootGrid" Background="#2C3E50">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Background="#1A252F" BorderBrush="#1ABC9C" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="PLAYERS" Foreground="#1ABC9C" FontWeight="Bold" Margin="10" HorizontalAlignment="Center" FontSize="14"/>
                <ItemsControl Grid.Row="1" ItemsSource="{Binding AllPlayers}" Margin="5">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#2C3E50" Margin="2" Padding="10" CornerRadius="4">
                                <StackPanel>
                                    <TextBlock Text="{Binding Profile.Name}" Foreground="White" FontWeight="SemiBold" FontSize="14"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock Text="Ping: " Foreground="#BDC3C7" FontSize="10"/>
                                        <TextBlock Text="{Binding Latency, StringFormat='{}{0}ms'}" Foreground="#1ABC9C" FontSize="10" FontWeight="Bold"/>
                                    </StackPanel>
                                    <!-- Future: Icon, Level -->
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>


        <!-- Animation Layer -->
        <Canvas x:Name="AnimationLayer" Grid.ColumnSpan="2" IsHitTestVisible="False"/>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding Status}" Foreground="White" FontSize="20" HorizontalAlignment="Center" Margin="5"/>

            <!-- Main Game Area -->
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Opponent Area -->
                <StackPanel Grid.Row="0">
                    <TextBlock Text="相手 (Opponent)" Foreground="White" HorizontalAlignment="Center" FontSize="12" Margin="0,5,0,0"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <core:CardControl x:Name="OpponentDeck" IsFaceUp="False" Margin="10,5">
                            <core:CardControl.Style>
                                <Style TargetType="core:CardControl">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding OpponentDeckCount}" Value="0">
                                            <Setter Property="Visibility" Value="Hidden"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </core:CardControl.Style>
                            <core:CardControl.ToolTip>
                                <TextBlock Text="{Binding OpponentDeckCount, StringFormat='山札: {0}枚'}"/>
                            </core:CardControl.ToolTip>
                        </core:CardControl>
                        
                        <ItemsControl x:Name="OpponentHandItems" ItemsSource="{Binding OpponentHandViewModels}" HorizontalAlignment="Center">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <core:CardControl IsFaceUp="False" Margin="5">
                                        <core:CardControl.Style>
                                            <Style TargetType="core:CardControl">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Card}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Hidden"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsAnimating}" Value="True">
                                                        <Setter Property="Visibility" Value="Hidden"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </core:CardControl.Style>
                                    </core:CardControl>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </StackPanel>

                <!-- Field Area -->
                <Grid Grid.Row="1" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <core:CardControl x:Name="Pile0" Suit="{Binding State.Piles[0].Suit}" Number="{Binding State.Piles[0].Number}" IsFaceUp="{Binding IsGameStarted}" Margin="15">
                            <core:CardControl.Style>
                                <Style TargetType="core:CardControl">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding State.ResetCountdown, Converter={StaticResource ResetVisibleConverter}}" Value="True">
                                            <Setter Property="Visibility" Value="Hidden"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </core:CardControl.Style>
                        </core:CardControl>
                        <core:CardControl x:Name="Pile1" Suit="{Binding State.Piles[1].Suit}" Number="{Binding State.Piles[1].Number}" IsFaceUp="{Binding IsGameStarted}" Margin="15">
                            <core:CardControl.Style>
                                <Style TargetType="core:CardControl">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding State.ResetCountdown, Converter={StaticResource ResetVisibleConverter}}" Value="True">
                                            <Setter Property="Visibility" Value="Hidden"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </core:CardControl.Style>
                        </core:CardControl>
                    </StackPanel>

                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding State.ResetCountdown, Converter={StaticResource ResetVisibleConverter}}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="{Binding State.ResetCountdown}" Foreground="White" FontSize="80" FontWeight="Bold" HorizontalAlignment="Center" Opacity="0.8">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="15" ShadowDepth="0" Color="Orange"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </StackPanel>
                </Grid>

                <!-- My Area -->
                <StackPanel Grid.Row="2">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <core:CardControl x:Name="MyDeck" IsFaceUp="False" Margin="10,5">
                            <core:CardControl.Style>
                                <Style TargetType="core:CardControl">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding MyDeckCount}" Value="0">
                                            <Setter Property="Visibility" Value="Hidden"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </core:CardControl.Style>
                            <core:CardControl.ToolTip>
                                <TextBlock Text="{Binding MyDeckCount, StringFormat='山札: {0}枚'}"/>
                            </core:CardControl.ToolTip>
                        </core:CardControl>

                        <ItemsControl x:Name="MyHandItems" ItemsSource="{Binding MyHandViewModels}" HorizontalAlignment="Center">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Click="CardButton_Click" Margin="5">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource CardButtonStyle}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Card}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Hidden"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsAnimating}" Value="True">
                                                        <Setter Property="Visibility" Value="Hidden"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                        <core:CardControl Suit="{Binding Card.Suit}" Number="{Binding Card.Number}" IsPlayable="{Binding IsPlayable}" IsFaceUp="{Binding DataContext.IsGameStarted, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                    <TextBlock Text="あなた (You)" Foreground="White" HorizontalAlignment="Center" FontSize="12" Margin="0,0,0,5"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
